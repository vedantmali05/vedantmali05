# Context: Code Formatting & Cleanup Guidelines

> **Living context for `role-code-sanitizer`**

---

## Code Hygiene Guidelines

1. Collapse unnecessary multi-line JS declarations and argument lists.
2. Remove debug console logging (`console.log`, `console.debug`).
3. Replace inline event handlers (`onclick`, `onchange`) with `addEventListener`.
4. Standardize utility function usage across modules.
